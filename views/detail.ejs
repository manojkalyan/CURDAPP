<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Record Detail</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
        }

        h2 {
            font-size: 24px;
            margin-top: 10px;
            margin-bottom: 20px;
        }

        p {
            margin-bottom: 10px;
        }

        .info-label {
            font-weight: bold;
        }

        .buttons {
            text-align: center;
            margin-top: 20px;
        }

        .button {
            background-color: #007bff;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            text-decoration: none;
            margin-right: 10px;
        }

        .button:hover {
            background-color: #0056b3;
        }

        /* Media Queries for Responsive Design */
        @media screen and (max-width: 600px) {
            .container {
                max-width: 100%;
                padding: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Record Detail</h1>
        <h2>Name: <%= record.name %></h2>
        <p class="info-label">Description:</p>
        <p><%= record.description %></p>
        <p class="info-label">Category:</p>
        <p><%= record.category %></p>
        <p class="info-label">Active:</p>
        <p><%= record.isActive ? 'Yes' : 'No' %></p>
        <div class="buttons">
            <a class="button" href="/records/recordList">Back to Records</a>
            <a class="button" href="/records/getrecordbyId/<%= record._id %>">Update</a>
            <a class="button" href="/records/delete/<%= record._id %>">Delete</a>
        </div>
    </div>
</body>
</html>
