<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 400px;
            margin: 80px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
        }

        label {
            display: block;
            margin-bottom: 5px;
        }

        input[type="text"],
        input[type="password"],
        select {
            width: 100%;
            padding: 8px; /* Smaller padding */
            margin-bottom: 10px; /* Smaller margin */
            border: 1px solid #ccc;
            border-radius: 3px;
            font-size: 14px; /* Smaller font size */
        }

        select {
            appearance: none;
        }

        .button {
            background-color: #007bff;
            color: #fff;
            padding: 8px 15px; /* Smaller padding */
            border: none;
            border-radius: 3px;
            font-size: 14px; /* Smaller font size */
            cursor: pointer;
            display: inline-block; /* Display as inline-block for button-like appearance */
            text-decoration: none; /* Remove underline from links */
            margin-right: 10px; /* Add margin between buttons */
        }

        .button:hover {
            background-color: #0056b3;
        }

        a {
            text-align: center;
            display: block;
            margin-top: 10px;
            text-decoration: none; /* Remove underline from links */
        }

        @media screen and (max-width: 600px) {
            .container {
                max-width: 100%;
                padding: 10px;
            }

            input[type="text"],
            input[type="password"],
            select {
                font-size: 12px; /* Smaller font size for smaller screens */
            }

            .button {
                font-size: 12px; /* Smaller font size for smaller screens */
                padding: 6px 10px; /* Smaller padding for smaller screens */
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>SignUp</h1>
        <form action="/user/createUser" method="POST">
            <label for="name">Name:</label>
            <input type="text" name="name" id="name">
            <label for="email">Email:</label>
            <input type="text" name="email" id="email">
            <label for="password">Password:</label>
            <input type="password" name="password" id="password">
            <label for="gender">Gender:</label>
            <select name="gender" id="gender">
                <option value="male">Male</option>
                <option value="female">Female</option>
                <option value="other">Other</option>
            </select>
            <button class="button" type="submit">Sign Up</button> <!-- Use a button element for Sign Up -->
        </form>
        <a class="button" href="/user/getLoginForm">Login</a> <!-- Style the link as a button -->
    </div>
</body>
</html>
